# Протокол контроллера гибкого диска

Arduino Uno держит дисковод и файловую систему. STM8S103K3 — мастер на шине I2C и мост к Радио-86РК. Своей FAT на STM8 нет.

Низкий уровень диска — [ArduinoFDC](https://github.com/dhansel/ArduinoFDC/tree/main) Дэвида Ханселя (David Hansel): чтение и запись секторов, геометрия 3,5″ и 5,25″. Команды каталога и файлов по I2C добавлены в этой плате (palmira original software).

Дисковод может быть 3,5″ или 5,25″. После включения тип — 3,5″ HD, 1,44 МБ. При смене дискеты тип снова ставится в 1,44 МБ. Мотор гаснет через 4 секунды после последней команды.

## Линия

| | |
|---|---|
| Адрес | `0x42`, 7 бит |
| STM8, корпус LQFP32 | PC1 = SCL, вывод 18. PC2 = SDA, вывод 19 |
| Arduino Uno | A4 = SDA, A5 = SCL |
| Уровни | обе платы 5 В, подтяжки SDA и SCL к +5 В (см. README) |
| Скорость | около 50–100 кГц |

Аппаратный I2C STM8 выключен. Ножки PB4 и PB5 заняты шиной данных Радио-86РК.

## Обмен

1. Мастер пишет команду: `START`, адрес `0x42` + запись, байты команды, `STOP`.
2. Мастер читает ответ: `START`, адрес `0x42` + чтение, ровно 32 байта, `STOP`.
3. Если первый байт `0xFF` (`BUSY`), пауза около 1 мс и шаг 2 снова.
4. Короткий ответ дополняется до 32 байт значением `0xFF`. Значимы только первые байты по формату команды.

Пока Arduino читает сектор или работает с FatFs, на каждый запрос отдаётся 32 байта `0xFF`. Один байт `0xFF` при чтении 32 байт вешает приёмник TWI, следующий ответ уже не приходит.

Ожидание статуса — около 0,2 с. Ожидание файловой команды — около 20 с. Если ответа нет, STM8 сам ставит код `0x1E`. Это не байт Arduino.

Путь — строка FAT, в конце нуль. После кода команды не больше 31 байта. Пустой путь каталога — корень `0:\`. Слэш допустим: `BOOT/SHELL.RK`.

## Коды статуса

Байт 0 ответа.

| Код | Имя | Смысл |
|-----|-----|--------|
| `0x00` | OK | команда выполнена; у чтения файла данные есть |
| `0x01` | ERROR | общая ошибка |
| `0x02` | NOTREADY | дисковод не готов |
| `0x03` | NOTFOUND | нет файла или каталога |
| `0x04` | EOF | конец файла или каталога |
| `0x05` | READONLY | диск защищён от записи |
| `0x06` | EXISTS | объект уже есть |
| `0x07` | INVALID | неверные аргументы или неизвестная команда |
| `0x08` | DENIED | отказ |
| `0x10` | MOREDATA | в чтении сектора будут ещё данные |
| `0x1N` | FatFs | ошибка файла, `N` — младшая тетрада `FRESULT` |
| `0xFF` | BUSY | команда ещё выполняется |

`0x1N` ставится при ошибке открытия или чтения, кроме «нет файла» и «нет пути»: те два случая дают `0x03`.

| Код | Смысл |
|-----|--------|
| `0x11` | ошибка чтения сектора |
| `0x12` | внутренняя ошибка FatFs |
| `0x13` | диск не готов |
| `0x16` | плохое имя |
| `0x17` | отказ |
| `0x18` | уже существует |
| `0x1A` | защита записи |
| `0x1B` | неверный диск |
| `0x1C` | том не смонтирован |
| `0x1D` | на дискете нет FAT |

## Команды

### Выбор дисковода — `0x01`

Запись: `[0x01, drive]`. `drive`: `0` — A, `1` — B.

Ответ: `[status]`.

### Статус — `0x02`

Запись: `[0x02]`.

Ответ, 6 байт:

| Смещение | Поле |
|----------|------|
| 0 | `0x00` |
| 1 | тип диска |
| 2 | `1` — носитель предполагается; ошибка вскрывается на открытии или чтении |
| 3 | `1`, если защита записи |
| 4 | `1`, если мотор крутится |
| 5 | выбранный дисковод, 0 или 1 |

Статус не крутит мотор и не ждёт индексного импульса.

| Код типа | Диск |
|----------|------|
| 0 | 5,25″ DD, 360 КБ |
| 1 | 5,25″ DD в HD-дисководе |
| 2 | 5,25″ HD, 1,2 МБ |
| 3 | 3,5″ DD, 720 КБ |
| 4 | 3,5″ HD, 1,44 МБ |

### Каталог — `0x10`, затем `0x11`

`0x10` открывает каталог. Запись: `[0x10, path…]`. Ответ: `[status]`. Мотор включается.

`0x11` читает следующую запись. Запись: `[0x11]`.

Пока записи есть, ответ 19 байт:

| Смещение | Поле |
|----------|------|
| 0 | `0x00` |
| 1…13 | имя, до 12 символов, затем `0` |
| 14 | атрибут FAT (`0x10` — каталог) |
| 15…18 | размер, младший байт первый |

Имена приводятся к верхнему регистру. Когда записей больше нет, ответ `[0x04]`, каталог закрывается.

### Файл

Открыт один файл. Новое открытие закрывает предыдущий.

| Команда | Код | Запись | Ответ |
|---------|-----|--------|--------|
| Открыть на чтение | `0x20` | `[0x20, path…]` | `[status]` |
| Читать | `0x21` | `[0x21, max]` | `[status, len, data…]` |
| Открыть на запись | `0x22` | `[0x22, path…]` | `[status]`, файл создаётся заново |
| Писать | `0x23` | `[0x23, len, data…]` | `[status]` |
| Закрыть | `0x24` | `[0x24]` | `[status]` |
| Удалить | `0x25` | `[0x25, path…]` | `[status]` |
| Позиция | `0x26` | `[0x26, offset, 4 байта LE]` | `[status]` |
| Размер | `0x27` | `[0x27]` | `[0x00, size, 4 байта LE]` |
| Где указатель | `0x28` | `[0x28]` | `[0x00, pos, 4 байта LE]` |
| Создать каталог | `0x29` | `[0x29, path…]` | `[status]` |

`max` и `len` не больше 30. Если чтение вернуло меньше, чем просили, статус `0x04` (конец файла), в `len` — сколько байт лежит следом.

Открытие на чтение включает мотор и монтирует том `0:`.

### Мотор — `0x40` / `0x41`

Запись: `[0x40]` включить, `[0x41]` выключить. Ответ: `[status]`.

Если мотор стоял, включение ждёт около секунды разгона. Без команды выключения мотор всё равно снимется через 4 секунды тишины на шине.

### Формат и геометрия

`0x30` — формат дорожек. Запись: `[0x30, fromTrack, toTrack]`. Ответ: `[status]`. Это низкоуровневая разметка, не создание FAT.

`0x31` — геометрия. Запись: `[0x31]`. Ответ: `[0x00, heads, tracks, sectors, drive_type]`.

### Сектор 512 байт

`0x50` — читать сектор. Запись: `[0x50, track, side, sector]`. Номер сектора с 1.

Дальше только чтение 32 байт, без новой команды, пока сектор не кончится. Кусок — 30 байт данных:

- не последний: `[0x10, data×30]`
- последний: `[0x00, data…]`

`0x51` — начать запись сектора. Запись: `[0x51, track, side, sector]`. Ответ: `[status]`. Данных ещё нет.

`0x52` — очередные байты сектора. Запись: `[0x52, data…]`. Повторять, пока не наберётся 512 байт. После последнего куска ответ — результат записи дорожки.

Буфер сектора совмещён с окном FatFs. Секторную передачу не перемежают с файловыми командами.

## Что из этого видит Радио-86РК

Команды каталога и файлов на шине КР580ВВ55 заданы BIOS Алексея Морозова. STM8 переводит их в команды I2C выше.

| Команда РК | Что делает BIOS | Команда I2C |
|------------|-----------------|-------------|
| 2 | загрузить `BOOT/SHELL.RK` | открыть и читать файл |
| 3 | каталог | `0x10`, затем `0x11` |
| 4 | открыть | `0x20` |
| 5 | позиция в файле | `0x26` |
| 6 | читать | `0x21` |
| 7 | писать | `0x23` |
| 8 | перенос | удаление и запись под новым именем |

Статус Arduino на экране становится кодом BIOS: «файл не найден» или `ошибка FDD` и байт в hex.

| Ответ I2C | Код на РК |
|-----------|-----------|
| `0x03` нет файла | `04` |
| `0x01`, `0x02` | `02` |
| `0x05`, `0x08` | `06` |
| `0x06` | `08` |
| `0x07` и прочие | `0C` |
| `0x1N` | тот же байт `1N` |
| нет ответа | `1E` |
